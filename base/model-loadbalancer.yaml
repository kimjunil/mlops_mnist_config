apiVersion: v1
kind: Service
metadata:
  name: mnist-classifier-svc
  labels:  
    app: mnist-classifier
spec:
  type: LoadBalance
  selector:  
    app: mnist-classifier
  ports:
  - port: 5000  
    targetPort: 80  
    protocol: TCP